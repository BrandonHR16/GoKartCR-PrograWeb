@using GoKartCR.Entities
@model dynamic

@{
    Layout = "_LayoutAdmin";
}

<div class="container">
    <div class="text-xl-center fs-2 ">Preguntas</div>
    <div class="card">
        <div class="card-body">
            <table class="" id="TablaPreguntas">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Pregunta</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if( Model != null){
                     @foreach (Pregunta pregunta in Model.listaDePreguntas)
                    {
                        <tr>
                        <td>@pregunta.idPregunta</td>
                        <td>@pregunta.nombreUsuario</td>
                        <td>@pregunta.correo</td>
                        <td>@pregunta.mensaje</td>
                        <td><button id="btnEnviar" class="btn btnResponder btnEnviar">Responder</button></td>
                    </tr>
                    }                       
        }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="modalMensaje">
    <div class="modal-dialog modal-dialog-centered modal-xl" role="">

        <div class="modal-content">
            <div class="modal-header">

                <h2 class="modal-title">Responder</h2>

                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                   
                </button>
            </div>

            <div class="modal-body">
                @using (Html.BeginForm("envioCorreo", "Administrador"))
                {
                    <div class="row gx-2">

                        <div class="col">
                            <div>
                                <label class="text-center">Correo</label>
                                @Html.TextBox("correo","",
                            new
                            {
                            @class="form-control",
                            @type="email",
                            @id="correoModal"           
                            })
                            </div>
                            <div>
                                <label class="text-center">Asunto</label>
                                @Html.TextBox("Asunto","",
                            new
                            {
                            @class="form-control",
                            @type="input",
                            @id="AsuntoModal"
                            })
                            </div>
                             <div>
                                <label class="text-center">Mensaje</label>
                                @Html.TextArea("Mensaje","",
                            new
                            {
                            @class="form-control",
                            @type="text",
                            @id="MensajeModal"
                            })
                            </div>
                            <div>
                                <button type="submit" class="form-control" id="submit-button" name="submit">Enviar Correo</button>
                            </div>
                        </div>

                    </div>
                }
            </div>
            </div>
            <div class="modal-footer"></div>

        </div>
    </div>

   
